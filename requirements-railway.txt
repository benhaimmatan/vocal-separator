# Railway-optimized requirements - Smaller image size
# Core web framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Audio processing (lightweight)
librosa==0.10.1
soundfile==0.12.1
audioread==3.0.1
numpy==1.24.3
scipy==1.11.4

# PyTorch CPU-only (smaller than full version)
torch==2.1.0+cpu
torchaudio==2.1.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# Chord detection essentials
essentia==2.1b6.dev1110
midiutil==1.2.1
scikit-learn==1.3.2

# API integrations
requests==2.31.0
beautifulsoup4==4.12.2
python-dotenv==1.0.0
supabase==2.0.2
modal>=0.65.0

# YouTube support
google-api-python-client>=2.100.0
yt-dlp>=2024.12.0

# Chord evaluation (lightweight)
mir_eval==0.7
sortedcontainers==2.4.0
pyyaml==6.0.1

# Note: Demucs removed for Railway - use Modal for vocal separation
# If needed locally: pip install demucs==4.0.1
